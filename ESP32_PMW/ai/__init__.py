"""Scratchpad. Bench harnesses and offline design tooling -- not the flight pipeline.

Gitignored on purpose: this is where experiments live, and nothing in `controller/`
may depend on it except `coil_thermal`, which the flight path refuses to arm without.
"""
